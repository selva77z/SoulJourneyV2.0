const express = require("express"); const cors = require("cors"); const app = express(); app.use(cors()); app.use(express.json()); app.get("/api/test", (req, res) => { res.json({ message: "API is working!", timestamp: new Date().toISOString(), status: "success" }); }); app.post("/api/horoscopes/generate", (req, res) => { res.json({ success: true, chart: { name: "Demo User", planets: [] }, message: "Demo horoscope generated" }); }); app.get("*", (req, res) => { res.json({ message: "SoulJourney API", endpoints: ["/api/test", "/api/horoscopes/generate"] }); }); module.exports = app;
